<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#000000">

    <!-- The frame. Every crop icon shares it; only the block inside differs. -->
    <path
        android:pathData="M4,4 H20 A1,1 0 0 1 21,5 V19 A1,1 0 0 1 20,20 H4 A1,1 0 0 1 3,19 V5 A1,1 0 0 1 4,4 Z"
        android:strokeWidth="2"
        android:strokeColor="@android:color/white" />

    <!-- Kept region, held to the right edge. Deliberately not autoMirrored: "right" is the
         right of the image, not of the reading direction. -->
    <path
        android:pathData="M13,7 H19 V17 H13 Z"
        android:fillColor="@android:color/white" />

</vector>
