<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#000000">

    <!-- The frame. Every crop icon shares it; only the block inside differs. -->
    <path
        android:pathData="M4,4 H20 A1,1 0 0 1 21,5 V19 A1,1 0 0 1 20,20 H4 A1,1 0 0 1 3,19 V5 A1,1 0 0 1 4,4 Z"
        android:strokeWidth="2"
        android:strokeColor="@android:color/white" />

    <!-- Kept region: the whole image. The other three place a block inside the frame to say which
         part survives; here it fills it, because none is cropped away. -->
    <path
        android:pathData="M6,7 H18 V17 H6 Z"
        android:fillColor="@android:color/white" />

</vector>
